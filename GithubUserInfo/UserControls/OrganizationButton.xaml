<UserControl x:Class="GithubUserInfo.UserControls.OrganizationButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GithubUserInfo.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="300">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Image x:Name="image_icon" HorizontalAlignment="Left" Height="100" VerticalAlignment="Top" Width="100"/>
        <ScrollViewer Margin="100,0,0,0" Height="100" HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Auto" VerticalAlignment="Top">
            <Label x:Name="label_orgname" Content="organization name" VerticalAlignment="Stretch" FontSize="24" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        </ScrollViewer>
        <TextBox x:Name="textbox_description" Margin="0,105,0,0" TextWrapping="Wrap" Background="Transparent" Text="Description" BorderThickness="0" IsReadOnly="True"/>
    </Grid>
</UserControl>
